name: Daily AI Brief

on:
schedule:
- cron: "0 22 * * *"
workflow_dispatch:

permissions:
contents: write
pages: write
id-token: write

jobs:
build:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout
    uses: actions/checkout@v4

  - name: Hello
    run: echo "Morning AI Brief running"
```
